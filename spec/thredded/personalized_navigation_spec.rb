# frozen_string_literal: true
require "spec_helper"

describe Thredded::PersonalizedNavigation do
  it "has a version number" do
    expect(Thredded::PersonalizedNavigation::VERSION).not_to be nil
  end
end
