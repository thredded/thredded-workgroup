gem 'test_after_commit', group: :test
gem 'thredded', github: 'red56/thredded', branch: 'integration-201609220900'

if ENV['TRAVIS']
  gem 'codeclimate-test-reporter', group: :test, require: false
end

group :debug do
  gem 'derailed'
  gem 'pry-rails'
  gem 'rack-mini-profiler'

  platform :mri do
    gem 'byebug'
    gem 'flamegraph'
    gem 'stackprof'
  end
end
